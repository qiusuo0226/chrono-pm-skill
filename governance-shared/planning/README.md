# planning/

升级方案设计草稿的临时存放处（Q-5：保留目录）。

**生命周期**

- 设计阶段：草稿必须放在本目录，命名为 `upgrade-plan-v{版本号}.md`，每周期限 1 个。禁止附加后缀、禁止拆成多个文件。
- 方案落地进 `upgrade-to-*.md` + 基线 + CHANGELOG 之后：**删除草稿或改 status 为已固化**。禁止把已落地正文长期留在本目录。
- 发布审计对这里除本 README 以外的 `.md` 打 **警告**（不阻断），避免再出现 v1.15/v1.16 方案残留无人管。

本目录不进分发包。
