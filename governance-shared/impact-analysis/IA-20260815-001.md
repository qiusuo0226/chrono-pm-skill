# Impact Analysis

## CR ID

CR-20260815-001（需求双视图与开发文档关联，1.19.1 → 1.20.0）

## Summary

引入需求双视图机制（view_business 派生聚合 + view_dev/原型链接挂 REQ 层新增 2 可选列）、开发侧 source_type 扩展（5 个 technical 类）、WF-2 需求上下文条件加载、词库开发侧分类/预筛懒加载、12 号 Excel 扩展列 U/V、19 号存在性巡检。核心红线：technical 类 ATOM 不参与 scope_scope 聚合。无 workspace schema 变更、无迁移；全部新增字段可选，存量工作区向后兼容。

## Affected Layers

| Layer | Affected | Notes |
|---|---|---|
| Contract Layer (SKILL.md / skill.json / contracts/) | yes | 仅版本/能力描述更新（SKILL.md front matter + §5.2、skill.json version/versionHistory/blueprint.lastVersion）；contracts/skill-contract.md 不动 |
| Execution Rules (references/01~16) | yes | 00/05/07/12/17/19 六份规则修改；07 号为主体（新增 §8.10） |
| Templates (assets/templates/) | yes | requirement-register（+2 列）、source-type-registry（+5 类型）、personal/project-daily（+可选节） |
| Scripts (scripts/) | yes | 仅 _version.py 版本号；无逻辑改动、无迁移脚本改动 |
| Workspace Schema | no | 保持 0.8.0，migrations 不新增 |
| Tests (tests/) | yes | regression-suite Module 37（DV-001~010）+ 统计表（合计 259） |

## Affected Capabilities

| Capability | Impact | Risk | Need Regression |
|---|---|---|---|
| daily_report | medium（WF-2 新增需求上下文条件加载，≤10 条 REQ，降级不阻塞） | 低 | yes（DV-004/005/006） |
| weekly_report | none | — | no |
| pm_daily_todo | none | — | no |
| quick_query | medium（新增需求详情/双视图路由；范围判定路由不变但增加 technical 排除约束） | 中（红线） | yes（DV-001/007） |
| output_artifact | none | — | no |
| continuity | none | — | no |
| resource_management | none | — | no |
| todo_snapshot | none | — | no |
| self_check | low（19 号新增存在性巡检维度，非强制） | 低 | yes（DV-010） |
| versioning | low（版本触点同步） | 低 | yes（audit_release.py 断言） |
| excel_generation | low（扩展列 U/V 追加，方案 α 不改既有列序） | 低 | yes（DV-003） |
| update_trigger | none | — | no |
| requirement_intelligence（RI 扩展） | high（scope_scope 聚合排除硬约束 + 触发 D + technical 归并 + 渐进导入） | 中（红线守护） | yes（DV-001/002/008） |
| domain_glossary | low（分类标签示例 + 预筛/懒加载 + 开发侧归一） | 低 | yes（DV-009） |

## Compatibility

- [x] 不影响旧工作区
- [ ] 需要迁移
- [ ] 需要更新 schema
- [ ] 需要手工确认

说明：requirement-register 新增 2 列均为可选（存量行无此列不影响解析与既有流程）；日报新节为可选节（无则跳过）；source-type-registry 为项目级可扩展表，追加不破坏既有行；Requirement Ref 保持 03 号可选语义，巡检仅存在性校验不变相强制。

## Rollback Plan

1. 从 `governance/baselines/1.19.1/` 恢复被修改的规则/模板/版本触点文件（references 6 份 + templates 4 份 + 版本触点 8 处）。
2. `tests/regression-suite.md` 移除 Module 37 段与统计表 Dual View 行，合计还原 249/161/88。
3. 运行 `python governance/scripts/audit_release.py` 验证版本触点一致性；存量工作区零操作（无迁移、新字段可选，用户已填的实现视图/原型链接数据保留不受影响，仅不再被流程消费）。
