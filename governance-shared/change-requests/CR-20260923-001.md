# Change Request

## Basic Info

| Field | Value |
|---|---|
| CR ID | CR-20260923-001 |
| Current Version | 3.30.3 → 4.0.0 |
| Current Workspace Schema | 0.17.0（不变） |
| Requester | PM / Agent A |
| Created At | 2026-09-23 |
| Change Type | architecture |
| Priority | P0 |
| Status | approved |
| Upgrade File | `ChronoPM-Project/governance/migrations/upgrade-to-4.0.0.md` |
| IA | `governance-shared/impact-analysis/IA-20260923-001.md` |

## Change Goal

对外只保留 ChronoPM-Project。查询收进 `query-skill/`，用脚本认说法并对单个文件做版本戳。Portfolio 收进 `portfolio-skill/`，不再单独发包。词库与各类别名并成一张表，升级程序处理旧数据，做不完不盖版本戳。

## Problem Statement

每次查询都先全量跑 `refresh_views.py`。口语、词库和别名分成多处。Portfolio 作为独立技能可以单独安装，但检索依赖 Project 写出的成员文件。

## Scope

`query-skill/`、`portfolio-skill/`、`query_locate.py`、说法表合并、`fact_stamps`、`SKILL.md` 集根改写、`migrate_workspace.py` 的 `needs_v400`、打包与审计改为单包。回归 Module 96。schema 保持 0.17.0。

## Non-goals

新建 `ai/wiki/`；接 Jev 云端接口；改联邦目录；把全部 Markdown 写入收成脚本；口语说法进入范围登记枚举；代更 Grok 安装区；本轮写入业务仓；商店下架。

## Acceptance Criteria

1. 查询脚本在说法表命中一条时只哈希该文件，不调用 `refresh_views.py --all`。
2. 旧词库行、`active-entities.json` 的 `alias_index`、工作包解析出的别名并进同一张表。少一键则不盖戳。
3. 集根不再提示安装 ChronoPM-Portfolio。手递写入仍指向成员根。
4. 打包只产出 Project zip。审计断言 13 按单包通过。
5. 仓库根不再有 `ChronoPM-Portfolio/SKILL.md`。
