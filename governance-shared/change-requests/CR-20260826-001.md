# Change Request

## Basic Info

| Field | Value |
|---|---|
| CR ID | CR-20260826-001 |
| Current Version | 3.15.0 → 3.16.0 |
| Current Workspace Schema | 0.16.0（不变） |
| Requester | PM / Agent A（用户授权分步执行；每节点 tag+双远程；全部完成后再核验） |
| Created At | 2026-08-26 |
| Change Type | contract_change / capability_change |
| Priority | P0 |
| Status | in_progress |
| Upgrade File | `ChronoPM-Project/governance/migrations/upgrade-to-3.16.0.md` |
| IA | `governance-shared/impact-analysis/IA-20260826-001.md` |
| AP | `governance-shared/planning/upgrade-plan-v3.16.0.md` V0.3.1（发布后删除） |

## Change Goal

1. 日期字段/区间两端强制完整 `YYYY-MM-DD`，禁止省年。
2. WP §8 执行人/排期字段边界（五列中只约束第 3、4 列；放行点名/AI聚合/待安排人）。
3. 计划 §4 投影完整性由 `verify_projection.py --check-plan-section4` 确定性校验（19 号默认不加该开关）。
4. 版本号只抄 VERSION + `.skill-version.json`；模板权威源=Skill 包 `assets/templates/`；skill_gap 禁建 manifest。
5. WP 可选 `related_wps`（YAML SSOT）；index 末尾加上游/下游两列。
6. index 派生 Mermaid 图仅对话输出（11 号 §17）。
7. 双包同号 3.16.0。

## Problem Statement

3.15.0 已有闸 2 与模板 13 阶段，但 AI 为控篇幅压缩 §4；执行人栏混入日期；省年区间；skill_gap 手写旧版本号并误建 manifest；WP 无法表达前后置责任链；大项目缺 index 派生图。

## Scope

见 `upgrade-to-3.16.0.md`。6 份 SG 一个 CR（用户明确捆绑，16 号 §4 例外）。

## Non-goals

新建独立规则文件；升 workspace schema；新建 Python 脚本（只扩展 verify_projection.py）；Mermaid 自动触发/落盘；WP 责任链自动阻塞；批量改业务工作区；改 23 号。

## Acceptance Criteria

1. 日期字段省年被标记；ID/叙事/周号不误伤。
2. 执行人栏含日期被标记；`(点名)` 合法。
3. `verify --check-plan-section4` 对缺阶段 PLAN 退出 1；默认巡检不加开关，压缩为 exit 2。
4. 新 skill_gap 无 manifest；版本号与 VERSION 一致。
5. related_wps 双向互指；废弃清对端；index 只追加末两列。
6. 「画图」加载 05+11，对话输出 Mermaid，不落盘。
7. 双包 3.16.0；schema 0.16.0；audit 退出码 0。

## Approval

- [x] approved（用户 2026-08-26：再审通过则直接升级，每步 tag+双远程，全部结束再检验）
