# Change Request

## Basic Info

| Field | Value |
|---|---|
| CR ID | CR-20260922-001 |
| Current Version | 3.30.2 → 3.30.3 |
| Current Workspace Schema | 0.17.0（不变） |
| Requester | PM / Agent A |
| Created At | 2026-09-22 |
| Change Type | fix |
| Priority | P1 |
| Status | approved |
| Upgrade File | `ChronoPM-Project/governance/migrations/upgrade-to-3.30.3.md` |
| IA | `governance-shared/impact-analysis/IA-20260922-001.md` |

## Change Goal

升级执行改由程序完成。所有 ChronoPM 工作区里已经拆过的标准文件，要串成「切片 → 来源指针和文档链接 → 工作包编号」。没串完不得改该工作区的 skillVersion。

## Problem Statement

3.30.2 的回填把页头合格当成成功，已绑可以一直空着，退出码仍为 0。触发还靠说明文字。一源一页没有把已拆标准文件串起来。

## Scope

会话钩子启动 `enforce_workspace_upgrade.py`。`stamp_skill_version()` 包装现有 `update_version_file()`。删掉迁移脚本里提前写 skillVersion 的那段。回填脚本在页头合格后仍检查已绑。`refresh_views.py` 的合格判定不改。20 号写明超时 120 秒和做不到绝对保证的情况。回归 Module 95。

## Non-goals

新建 `ai/wiki/`；`[[链接]]`；新造需求或工作包；模糊匹配标题；升 schema；日常 refresh 重写主题页；改 05 查询；代更 Grok 安装区；本轮写入业务仓。

## Acceptance Criteria

1. 已拆源对不上需求或没有工作包时，退出码非 0，skillVersion 不变。
2. 标题完全一致且其中一份已挂需求和工包时，另一份写入同一条来源指针，已绑含工包编号。
3. 还没拆、登记里也没有编号的源，已绑为 `无登记边`。
4. 技能版本低于工作区时不写文件。
5. `refresh_views.py --all` 仍不重写 `_digest.md`。
6. 不出现 `ai/wiki/`。

## Approval

- [x] approved（用户 2026-09-22：执行升级吧）
