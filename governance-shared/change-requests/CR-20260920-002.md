# Change Request

## Basic Info

| Field | Value |
|---|---|
| CR ID | CR-20260920-002 |
| Current Version | 3.30.0 → 3.30.1 |
| Current Workspace Schema | 0.17.0（不变） |
| Requester | PM / Agent A |
| Created At | 2026-09-20 |
| Change Type | fix |
| Priority | P1 |
| Status | approved |
| Upgrade File | `ChronoPM-Project/governance/migrations/upgrade-to-3.30.1.md` |
| IA | `governance-shared/impact-analysis/IA-20260920-002.md` |

## Change Goal

补上主题页过期检测的原件指纹层；写死无 Python 属性串；`missing_page` 巡检提示不阻断；skip 比较去掉 `as_of`。

## Problem Statement

`collect_source_digest_status` 只比切片，页头 `source_fingerprint` 改错仍 ok。属性串格式只在代码里。19 号未写 missing_page。每天首次 --all 因 as_of 改写 state。

## Scope

refresh_views.py；digest-schemas；05；19；tests SW-017 + 脚本用例。

## Non-goals

重开栏目骨架；升 schema；migrate；补 step tag。

## Acceptance Criteria

1. 页头原件指纹 ≠ ledger 列 → stale / reason=source。
2. 无 Python 串格式写在 digest-schemas，可逐字节复现。
3. missing_page 巡检提示不阻断。
4. 仅 as_of 不同不触发 source_digest_status 写盘。

## Approval

- [x] approved（用户 2026-09-20：该纠正的就纠正）
