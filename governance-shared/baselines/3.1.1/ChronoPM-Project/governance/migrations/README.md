# ChronoPM-Project 升级执行文件（包内）

本目录**只保留当前发布所需的一份** `upgrade-to-{version}.md`（上一发布版 → 当前版）。

完整历史链（0.2.0～上一版）归档在开发仓：

`governance-shared/migrations-history/`

分发包按 Q16 只带本目录，避免把 49+ 份历史打进 zip。

当前文件：`upgrade-to-3.1.1.md`（3.1.0 → 3.1.1，CR-G 三目录重组）。
