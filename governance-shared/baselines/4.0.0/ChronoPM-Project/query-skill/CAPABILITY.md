# query-skill — 能力目录（不是独立 Skill）

当前版是 ChronoPM-Project 内的能力目录，**禁止**命名为 `SKILL.md`。宿主不得把本目录扫成第二个 Skill。

- **何时加载**：单项目查询。SKILL.md「简单查询」行加载 `references/query-rules.md`。
- **脚本**：`scripts/query_locate.py`。提问只认说法、只哈希命中的一个文件。
- **不负责**：日报、待办写入、全量 `refresh_views.py`。
